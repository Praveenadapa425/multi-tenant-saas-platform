# Production Environment Variables
# Copy this file to .env and update values for production deployment

# Database Configuration
DB_HOST=database
DB_PORT=5432
DB_NAME=saas_db
DB_USER=postgres
DB_PASSWORD=secure_postgres_password_change_in_production

# JWT Configuration - MUST be changed in production
JWT_SECRET=very_secure_jwt_secret_key_must_be_at_least_32_characters_long

# Frontend Configuration
REACT_APP_API_URL=https://your-domain.com/api